/**
 * SCR-06 Coordinator dashboard (role_coordinator, role_admin) - on-brand
 * stub, TASK-026. The real load heatmap, approval queue, and reasoning
 * stream (FR-09, FR-10, FR-12) are a follow-on task.
 */
import { StubScreen } from './StubScreen';

export function CoordinatorDashboardScreen() {
  return <StubScreen titleKey="console.screen.dashboard.title" />;
}
